<% provide(:title, 'Edit user') %>
<% provide(:url, user_path) %>
<% provide(:button_text, '更新する') %>

<div class="container">

<%= render 'shared/flash' %>

  <div class="row">
    <div class="col-12">
      <h1>登録情報の編集</h1>
      <%= render 'form' %>
    </div>
  </div>
</div>